import React from 'react'
import styled from 'styled-components'

const StyleTextButton = styled.h4`
  font-size: 16px;
  font-weight: 700px;
  color: white;
`

const TextButton = () => {
  return <StyleTextButton></StyleTextButton>
}

export default TextButton
