import React from 'react'
import styled from 'styled-components'

const StyleTitle = styled.h2`
  font-size: 22px;
  font-weight: 700;
`

const Title = () => {
  return <StyleTitle></StyleTitle>
}

export default Title
